# r/R Operator is used to specify the raw string.

''' Raw strings are used in the cases where we need to print the actual escape characters such as "C://python".
To define any string as a raw string, the character r or R is followed string. '''

print(R"C:\python")