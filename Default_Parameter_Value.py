# Default Parameter Value (If we call the function without argument, it uses the default value)

def map(country="Norway"):
    print("I am from " + country)

    
map("Sweden")
map("India")
map()
map("Brazil")
