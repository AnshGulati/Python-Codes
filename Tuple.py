#Tuples are ordered sequence that are written as a comma-separated elements within round brackets.

'''Tuples once created can’t be modified that makes them faster as compared to 
    list. A tuple is a collection which is ordered and unchangeable.
'''

Score = (20,30,55,68,12,32)

#All the different types
Tuple1 = ("person", 20, 12.5)

#Tuples allow duplicate values.
thistuple = ("apple", "banana", "cherry", "apple", "cherry")
print(thistuple)